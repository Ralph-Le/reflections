To create the metadata platform, it creats .git and the metadata

The staging are remembers which files are to be tracked during the next commit and which to ignore. One can work with the staging area easily without mudlding up the commit.

Add those files that logical belogn to a commit and compare them before the commit.

Branche lead to well different branches where different things can be done with out interacting and/or messing up the other branch.

Diagramms help us see the branch sturcte, how the branches are related and if there are untracked changes.

Merging two branches, merges one branch (specified) into the other (current) and adds to the current branch everyhting added from parent but not yet in this branch. We represent it to show that the new branch has two parents.

Well pro: git doenst do anything it needs to do con add unnecessary stuff